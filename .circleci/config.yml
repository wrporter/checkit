version: 2.1

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: .ci/deploy-all.sh
      - run: e2e/.ci/build.sh
      - run: e2e/.ci/run.sh
      - run: e2e/.ci/copy-artifacts.sh
      - store_test_results:
          path: e2e/results
      - store_artifacts:
          path: e2e/results

workflows:
  build:
    jobs:
      - build:
          context: deploy
